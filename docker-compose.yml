version: "3"
services:
        webapp:
                build: .
                ports:
                        - "80:80"
        mongo:
                image: mongo
                ports:
                        - 27017:27017
